#ifndef _MAS_UTIL_H
#define _MAS_UTIL_H

#include "types.definition.h"

int autoWorkMAS(int argc, char **argv);
Executed assembly(Program program);
Program imassembly(Executed program);
QuadroWord run(Executed program);

#endif
